{
  "experimentalFeaturesEnabled": {
    "desiredStateConfiguration": true, //will probably always be required for DSC
    "moduleExtensionConfigs": true // temporary workaround for version split
  },
  "extensions": {
    "dsc": "./out/dsc.tgz" //only loads dsc extension for bicep 
  },
  "implicitExtensions": [] //essentially disables implicit extension (like azure extensions)
}

// ARMv2 changes from resource list => resource dict (this needed rework in dsc)
// DSC denies unknown fields which causes issues if unknown fields are present 