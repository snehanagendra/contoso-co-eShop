$schema: "1.0"
name: "parent-image-definition"
image: microsoftvisualstudio_visualstudioplustools_vs-2022-ent-general-win11-m365-gen2

parameters:
  message:
    type: 'string'
    default: 'Hello from parent-image-definition'
    required: false

tasks:
  - name: bicepconfig
    displayName: "Configure Configuration for DSC"
    description: Create bicepconfig.json file
    parameters:
      bicepConfigDirectoryPath: C:\ProgramData\Microsoft\DevBoxAgent\ImageDefinitions

  - name: powershell
    parameters:
      command: echo {{message}}
